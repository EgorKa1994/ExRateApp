import React from 'react';
import ReactDOM from 'react-dom';
import { ExRateApp } from '../src/Components/ExRateApp';

ReactDOM.render(
  <React.StrictMode>
    <ExRateApp />
  </React.StrictMode>,
  document.getElementById('root')
);
